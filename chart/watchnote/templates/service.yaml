apiVersion: v1
kind: Service
metadata:
  name: {{ include "watchnote.fullname" . }}
  labels:
    {{- include "watchnote.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "watchnote.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
